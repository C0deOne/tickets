{
  "name": "tickets",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "test:local": "astro build && serve dist",
    "test:prod": "cross-env NODE_ENV=production astro build && serve dist"
  },
  "dependencies": {
    "astro": "^5.10.1",
    "resend": "^4.6.0"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "autoprefixer": "^10.4.21",
    "cross-env": "^7.0.3",
    "gh-pages": "^6.3.0",
    "serve": "^14.2.4",
    "tailwindcss": "^3.4.17"
  }
}